# 2026-07-08

- git commit f229b37：权限模型升级为三层（readonly/default/full = L1/L2/L3），对标 WorkBuddy；permissions.ts 在 readonly 模式拦截所有写工具；index.html 权限菜单增加"仅阅读"选项
- 记忆存储重构：store.ts 支持 user/working/project 三 scope，新增 5min TTL 缓存（写操作自动失效），新增 recallWorkingMemory / recallAllMemories / clearWorkingMemory / listAllMemories 方法
- 修复 bug：默认权限下确认对话框首次不显示、切 chat 再回来才出现。根因是 tool_confirmation 事件直接 push queue，而 SSE 建立后只订阅 provider.onEvent 不再 splice queue。修复：provider 新增 emitEvent 方法广播事件走 listeners 通路；confirm handler 改用 provider.emitEvent；SSE/polling 两处 splice queue 时过滤已 resolved 的 tool_confirmation 防止重连重复弹框。commit 327a6e2
- 启动方式：deno task desktop 会编译打包 pi-a.app 并签名，但打包后父进程退出不自动开窗，需 `open pi-a.app` 启动；运行进程名为 laufey_webview
- Sprint 2 接续开发完成（zcode 限额后接续，4 个功能）：
  - 功能7 记忆分层补完：engine.ts loadMemoryPrompt 分层注入 working_memory + user_memory（MAX_USER_CHARS=10000）；memory.ts 工具加 scope 参数（user/working/all）；前端记忆面板加"范围"列+选择器；GET /api/memories 改用 listAllMemories
  - 功能14 权限三层收尾：permissions.ts 注释更新三层模型+DANGER_TOOLS 预留；engine __perm 类型修正
  - 功能6 会话分叉：db.ts 加 messages.parent_id 列 + conversation_forks 表；session store 加 forkConversation + appendMessage 支持 parentId；provider ensureSession 重放历史进 agent state（修复旧会话续聊失忆）；main.ts POST /api/conv/:id/fork；renderer 消息 hover 分叉按钮
  - 功能10 技能编辑器：新建 src/domains/skill/node/store.ts（CRUD ~/.pi-a/skills/，解析 frontmatter）；main.ts /api/skills GET/POST/PUT/DELETE；renderer 设置面板加"技能管理"tab（列表+编辑器+新建/保存/删除）
  - 注意：工作记忆当前全局共享，P2 加 session_id 隔离
  - commit 32a9244（Sprint2 全部完成）
- Sprint 3 部分完成（功能8 自动化 + 功能17 文件快照）：
  - 功能8 自动化系统：db.ts 加 automations + automation_runs 表；新建 src/domains/automation/node/store.ts（CRUD+运行记录）+ scheduler.ts（cron 解析 matchCron/nextCronTime，30s 扫描定时触发，触发时创建临时会话 provider.prompt）；main.ts /api/automations CRUD + /:id/runs + /:id/run 手动触发 + 启动 startScheduler；renderer 侧栏"自动化"分类视图（卡片列表+新建表单含 cron 预设+开关/立即运行/运行记录/删除）
  - 功能17 文件快照沙箱：db.ts 加 file_snapshots 表；新建 src/infra/file_snapshot.ts（snapshotFile 复制原文件到 ~/.pi-a/snapshots/，listFileSnapshots/listSnapshottedFiles/revertFileSnapshot，detectFileWrites 启发式检测 > >> tee sed -i）；engine.ts beforeToolCall 在 bash 放行前对检测到的文件自动快照；main.ts /api/file-snapshots GET + /:id/revert；renderer 设置面板加"文件快照"tab（文件列表+快照列表+回滚）
  - 功能9 速唤浮窗 + 功能13 Computer Use：未做，依赖外部能力（Deno Desktop 全局热键 API 不确定需 spike；Computer Use 依赖 cliclick + 辅助功能权限），留待下轮专门 spike
  - commit 86191ac（Sprint3 部分完成：自动化+文件快照）
- 功能13 Computer Use 完成（参考 WorkBuddy 05 文档§三 感知-操作循环范式）：
  - 新建 src/agent/tools/os.ts：screenshot（screencapture 截图返回 image base64）/ mouse_click / mouse_move / key_type（cliclick）/ app_focus（osascript）/ cursor_pos
  - tools/index.ts：Craft 模式注册 6 个 os 工具
  - permissions.ts：WRITE_TOOLS 加 mouse_click/move/key_type（default 确认）；新增 DANGER_TOOLS={mouse_click,key_type}，full 权限下也强制确认
  - engine.ts：SYSTEM_PROMPT 加工具说明 + <computer_use> 感知-操作循环指引
  - 依赖：screencapture（macOS 自带）；cliclick（brew install，已后台装）；权限需屏幕录制+辅助功能授权
  - 注意：screenshot 返回 ImageContent，需多模态模型识图（默认 deepseek-v4-flash 可能不支持，需配多模态模型）
  - commit da11767
  - 补全步数上限：permissions.ts 加 computerUseCount session 计数器，对 COMPUTER_USE_ACTION_TOOLS（mouse_click/mouse_move/key_type）计数，超 20 步拒绝（防失控循环，05文档§三）；screenshot/cursor_pos 感知类不计；main.ts /api/prompt 每次重置计数（新任务）；commit 7b3d080
- Sprint 4 功能11 docx自由编辑+pptx编辑（对标 WorkBuddy 子系统⑤+⑪，08计划功能11）：
  - editor.ts 新增 editDocxFree（pizzip 操作 word/document.xml：replace_text/insert_paragraph/delete_paragraph）+ editPptx（pizzip 操作 slideN.xml：replace_text）；保真度高，duplicate_slide 留 P2
  - doc.ts 新增 edit_docx_free + edit_pptx 工具（编辑前 createBackupVersion 备份）
  - tools/index.ts 注册；commit c5030de
- Sprint 4 功能12 PDF导出（对标 WorkBuddy 服务端渲染，08计划功能12）：
  - main.ts POST /api/export-pdf { path }：检测 soffice（/Applications/LibreOffice.app 或 /opt/homebrew/bin/soffice），soffice --headless --convert-to pdf 转换 docx/xlsx/pptx，登记为新工件，未装返回友好错误
  - renderer 工件预览弹窗加"导出 PDF"按钮（仅 docx/xlsx/pptx 显示，导出中 loading）
  - 当前环境 soffice 在 /opt/homebrew/bin/soffice 可用；commit 9086332
- Sprint 4 功能18 RAG向量召回（对标 WorkBuddy 云端 embedding，08计划功能18）：
  - pi-ai 无 embedding API，MVP 用 TF-IDF + 余弦相似度（本地无依赖）；P2 升级 embedding
  - db.ts doc_chunks 表；新建 src/domains/rag/node/store.ts（indexDoc 分块800/重叠100 + 中文bigram/英文单词去停用词 + searchChunks 余弦检索 topK）
  - 新建 src/agent/tools/rag.ts：index_doc + search_docs 工具
  - tools/index.ts 注册；commit 6d85b1d
- Sprint 4 功能15 会话恢复（对标 WorkBuddy 子系统②+⑩，08计划功能15）：
  - load 回放（会话分叉时已实现）：provider.ensureSession 从 DB 重放历史进 agent.state.messages
  - resume 标识：会话列表 running 显示"未完成"badge、failed 显示"失败"badge；点击未完成会话 load 历史 + 可继续聊
  - checkpoint 文件暂不做（messages 在 DB 够了；agent 运行态=LLM流崩溃不可恢复，只能标记+重发）
  - commit abc581d
- Sprint 4 全部完成（功能11/12/18/15）
